CREATE TABLE IF NOT EXISTS Users (
    	ID int AUTO_INCREMENT PRIMARY KEY,
    	Username varchar(8),
    	Password varchar(32),
    	Admin bit
)


INSERT INTO `users`( `Username`, `Password`, `Admin`) VALUES ('admin','admin',1);

SELECT COUNT(Username) as counter FROM `users` WHERE Username='admin'